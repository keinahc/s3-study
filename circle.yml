machine:
  node:
    version: 6.11.2
  post:
    - npm install -g npm@5.3
general:
  branches:
    only:
      - master
deployment:
  production:
    branch: master
    commands:
      - npm run deploy